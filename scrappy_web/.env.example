# ScrapPY Web Configuration
# Copy this file to .env and fill in the values

# REQUIRED: Secret key for JWT signing (min 32 chars)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SCRAPPY_SECRET_KEY=

# Optional: Token expiration in minutes (default: 30)
SCRAPPY_TOKEN_EXPIRE_MINUTES=30

# Optional: Max upload file size in MB (default: 10)
SCRAPPY_MAX_FILE_SIZE_MB=10

# Optional: Rate limits
SCRAPPY_LOGIN_RATE_LIMIT=5/minute
SCRAPPY_JOB_RATE_LIMIT=10/minute

# Optional: Upload directory path
# SCRAPPY_UPLOAD_DIR=/path/to/uploads
